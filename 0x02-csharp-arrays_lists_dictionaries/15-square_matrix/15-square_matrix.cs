using System;

class Program
{
    public static int[,] Square(int[,] myMatrix)
    {
        int[,] newmatrix = new int[myMatrix];
        
        for (int x = 0; x < myMatrix.Length)
    }
}

